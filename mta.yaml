_schema-version: "3.2"
ID: ns.zprueb2.zprueb2
version: 0.0.1

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces

modules:
  - name: ns.zprueb2.zprueb2
    type: staticfile
    path: dist
    parameters:
      disk-quota: 256M
      memory: 256M